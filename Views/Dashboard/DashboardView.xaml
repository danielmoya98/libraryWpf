<UserControl x:Class="library.Views.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:library.Controls">

    <ScrollViewer VerticalScrollBarVisibility="Hidden"
                  HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="0 8 8 8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- KPIs -->
                <RowDefinition Height="*"/>     <!-- Charts -->
            </Grid.RowDefinitions>

            <!-- KPIs -->
            <controls:StatsSummary Grid.Row="0" ItemsSource="{Binding Stats}"/>

            <!-- Charts responsivos -->
            <controls:ChartSummary Grid.Row="1"
                                   ItemsSource="{Binding Charts}"
                                   Breakpoints="1600=3;1000=2;0=1"/>
        </Grid>
    </ScrollViewer>
</UserControl>