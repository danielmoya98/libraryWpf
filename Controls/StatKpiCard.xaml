<!-- Controls/StatKpiCard.xaml -->
<UserControl x:Class="library.Controls.StatKpiCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/iconpacks"
             MinWidth="240" Height="92"
             HorizontalAlignment="Stretch">
  <materialDesign:Card Padding="14"
                       Background="{DynamicResource Brush.Card}"
                       BorderBrush="{DynamicResource Brush.Border}"
                       BorderThickness="1"
                       UniformCornerRadius="12">
    <Grid>
      <!-- Icono -->
      <Border Width="28" Height="28" CornerRadius="6"
              Background="{DynamicResource Brush.Hover}"
              HorizontalAlignment="Right" VerticalAlignment="Top">
        <mah:PackIconMaterial Kind="{Binding Icon}" Width="16" Height="16"
                              Foreground="{DynamicResource Brush.Primary}"
                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
      </Border>

      <!-- Texto -->
      <Grid Margin="0,4,0,0">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Title}"
                   Foreground="{DynamicResource Brush.TextMuted}"
                   FontSize="12" FontWeight="SemiBold"/>

        <TextBlock Grid.Row="1"
                   Text="{Binding Value}"
                   FontSize="22" FontWeight="Bold"
                   Foreground="{DynamicResource Brush.Text}"
                   Margin="0,2,0,0"/>

        <TextBlock Grid.Row="2"
                   Text="{Binding ThisMonth, StringFormat={}{0} este mes}"
                   Foreground="{DynamicResource Brush.TextMuted}"
                   FontSize="12" Margin="0,6,0,0"/>
      </Grid>
    </Grid>
  </materialDesign:Card>
</UserControl>
