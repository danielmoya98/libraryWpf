<UserControl x:Class="library.Dialogs.ConfirmDeleteBookDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:converters="clr-namespace:library.Converters"
             MinWidth="520" MaxWidth="680" MaxHeight="420">

  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <!-- Reusa tu paleta/estilos -->
        <ResourceDictionary Source="../Resources/Themes/CustomersTable.Styles.xaml"/>
      </ResourceDictionary.MergedDictionaries>

      <converters:SafeImageSourceConverter x:Key="SafeImg" />

      <!-- Botón peligro (rojo) -->
      <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="#E53935"/>
      </Style>
    </ResourceDictionary>
  </UserControl.Resources>

  <Border Padding="22" Background="White">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <!-- Título -->
      <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,14">
        <mah:PackIconMaterial Kind="TrashCanOutline" Width="20" Height="20" Foreground="#E53935" />
        <TextBlock Text="Eliminar libro" FontSize="20" FontWeight="SemiBold"
                   Foreground="#243147" Margin="8,0,0,0"/>
      </StackPanel>

      <!-- Cuerpo -->
      <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="180"/>
          <ColumnDefinition Width="20"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Portada -->
        <Border Grid.Column="0" Width="140" Height="200" CornerRadius="8"
                BorderBrush="#E2E8F0" BorderThickness="1" Background="#F6F8FB"
                HorizontalAlignment="Center" VerticalAlignment="Top">
         
        </Border>

        <!-- Datos + advertencia -->
        <StackPanel Grid.Column="2" Margin="0,0,0,0">
          <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="16"
                     TextWrapping="Wrap" />
          <TextBlock>
            <Run Text="por " Foreground="#6B7280"/>
            <Run Text="{Binding Author}"/>
          </TextBlock>

          <StackPanel Orientation="Horizontal" Margin="0,8,0,0" >
            <StackPanel>
              <TextBlock Text="ISBN" Foreground="#6B7280" FontSize="12"/>
              <TextBlock Text="{Binding Isbn}" />
            </StackPanel>
            <StackPanel>
              <TextBlock Text="Categoría" Foreground="#6B7280" FontSize="12"/>
              <TextBlock Text="{Binding Category}" />
            </StackPanel>
            <StackPanel>
              <TextBlock Text="Año" Foreground="#6B7280" FontSize="12"/>
              <TextBlock Text="{Binding Year}" />
            </StackPanel>
          </StackPanel>

          <Border Background="#FFF3E0" BorderBrush="#FFE0B2" BorderThickness="1"
                  CornerRadius="8" Padding="10" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal">
              <mah:PackIconMaterial Kind="AlertOctagonOutline" Width="20" Height="20"
                                    Foreground="#E65100" Margin="0,0,8,0"/>
              <TextBlock Text="Esta acción no se puede deshacer. Se eliminará el libro del catálogo."
                         Foreground="#4E342E" TextWrapping="Wrap" />
            </StackPanel>
          </Border>
        </StackPanel>
      </Grid>

      <!-- Footer -->
      <DockPanel Grid.Row="2" LastChildFill="False" Margin="0,16,0,0">
        <!-- Cancelar -->
        <Button DockPanel.Dock="Right" Margin="8,0,0,0"
                Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                CommandParameter="{x:Null}">
          <StackPanel Orientation="Horizontal">
            <mah:PackIconMaterial Kind="Close" Width="18" Height="18" Margin="0,0,8,0"/>
            <TextBlock Text="Cancelar"/>
          </StackPanel>
        </Button>

        <!-- Eliminar -->
        <Button DockPanel.Dock="Right" Style="{StaticResource DangerButton}"
                Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                CommandParameter="True">
          <StackPanel Orientation="Horizontal">
            <mah:PackIconMaterial Kind="TrashCanOutline" Width="18" Height="18" Margin="0,0,8,0"/>
            <TextBlock Text="Eliminar"/>
          </StackPanel>
        </Button>
      </DockPanel>
    </Grid>
  </Border>
</UserControl>
