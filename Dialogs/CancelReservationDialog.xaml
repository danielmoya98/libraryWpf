<UserControl x:Class="library.Dialogs.CancelReservationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/iconpacks"
             MinWidth="460" MaxWidth="640" MaxHeight="420">

  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Resources/Themes/CustomersTable.Styles.xaml"/>
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </UserControl.Resources>

  <Border Padding="22" Background="White">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <!-- Título -->
      <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
        <mah:PackIconMaterial Kind="AlertCircleOutline" Width="20" Height="20" Margin="0,0,8,0" Foreground="#EF4444"/>
        <TextBlock Text="Cancelar reserva" FontSize="20" FontWeight="SemiBold" Foreground="#243147"/>
      </StackPanel>

      <!-- Cuerpo -->
      <StackPanel Grid.Row="1">
        <TextBlock Text="¿Seguro que deseas cancelar esta reserva?" Margin="0,0,0,8"/>
        <TextBlock Text="{Binding Reservation.BookTitle}" FontWeight="SemiBold"/>
        <TextBlock Text="{Binding Reservation.MemberName}" Foreground="#6B7280" Margin="0,2,0,10"/>

        <TextBox materialDesign:HintAssist.Hint="Motivo (opcional)"
                 Text="{Binding Reason}" AcceptsReturn="True" Height="100" TextWrapping="Wrap"/>
      </StackPanel>

      <!-- Footer -->
      <DockPanel Grid.Row="2" LastChildFill="False" Margin="0,16,0,0">
        <Button DockPanel.Dock="Right" Margin="8,0,0,0"
                Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                CommandParameter="{x:Null}">
          <StackPanel Orientation="Horizontal">
            <mah:PackIconMaterial Kind="Close" Width="18" Height="18" Margin="0,0,8,0"/>
            <TextBlock Text="Volver"/>
          </StackPanel>
        </Button>

        <Button DockPanel.Dock="Right"
                Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                CommandParameter="{Binding}">
          <StackPanel Orientation="Horizontal">
            <mah:PackIconMaterial Kind="DeleteForeverOutline" Width="18" Height="18" Margin="0,0,8,0"/>
            <TextBlock Text="Confirmar cancelación"/>
          </StackPanel>
        </Button>
      </DockPanel>
    </Grid>
  </Border>
</UserControl>
